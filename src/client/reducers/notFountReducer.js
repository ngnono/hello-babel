/**
 * Created by ngnono on 17-2-17.
 */

function reducer(state = {}, action = {type: 'default', payload: {}}) {
  // "use strict";

  switch (action.type) {

    default :
      return state;

  }

}

export default reducer;
